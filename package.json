{
  "private": true,
  "scripts": {
    "start": "node index.js",
    "lint": "eslint . && stylelint *.css **/*.css && htmllint *.html",
    "test": "npm run lint && mocha tests/*.js --timeout 3000 --exit"
  },
  "dependencies": {
    "express": "^4.16.2",
    "hbs": "^4.0.1",
    "htmllint-cli": "0.0.6",
    "metaweather": "0.0.2",
    "moment": "^2.21.0",
    "newsapi": "^2.2.0",
    "request": "^2.83.0"
  },
  "devDependencies": {
    "eslint": "^4.18.2",
    "eslint-config-hrundel": "^1.3.0",
    "mocha": "^5.0.4",
    "stylelint": "^7.13.0",
    "stylelint-config-hrundel": "^1.0.3",
    "supertest": "^3.0.0"
  }
}
