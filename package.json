{
  "private": true,
  "scripts": {
    "start": "node index.js",
    "lint": "eslint .",
    "test": "npm run lint && mocha --exit --timeout 5000 tests/*.js"
  },
  "dependencies": {
    "axios": "^0.18.0",
    "config": "^1.30.0",
    "express": "^4.16.2",
    "hbs": "^4.0.1",
    "morgan": "^1.9.0",
    "newsapi": "^2.2.0"
  },
  "devDependencies": {
    "eslint": "^4.8.0",
    "eslint-config-hrundel": "^1.3.0",
    "mocha": "^5.0.1",
    "supertest": "^3.0.0"
  }
}
