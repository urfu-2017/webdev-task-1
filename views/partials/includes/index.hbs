{{#> layouts/page}}
    <main class="index-main">
        <h2 class="index-main__heading">Категории новостей</h2>
        <ul class="categories">
            {{#each categories}}
                <li class="categories__item">
                    <a href="{{url}}" class="categories__link categories__link_main-theme">{{name}}</a>
                </li>
            {{/each}}
        </ul>
    </main>
{{/ layouts/page}}
