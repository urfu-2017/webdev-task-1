{{#if categories.length}}
    <ul>
        {{#each categories}}
            <li><a href="/categories/{{ name }}">{{ title }}</a></li>
        {{/each}}
    </ul>
{{^}}
    <p>Categories not found!</p>
{{/if}}