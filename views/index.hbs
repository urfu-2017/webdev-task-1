<!DOCTYPE html>
<html lang={{ lang }}>
<head>
    <meta charset="{{ meta.charset }}">
    <title>{{ title }}</title>
</head>
<body>
    {{> header}}
    {{> weather}}
    {{> newsCategories}}
    {{> footer}}
</body>
</html>