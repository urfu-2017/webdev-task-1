<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="{{ meta.charset }}">
    <title>{{ title }}</title>
    <meta name="description" content="{{ meta.description }}">
    <link rel="stylesheet" type="text/css" href="index.css">
</head>
<body>
    <div class="content-wrapper">
        <header class="header">
            <h1 class="header__text">Крутое название</h1>
        </header>

        {{> weather }}

        {{> categories }}

        {{> footer }}
    </div>
</body>