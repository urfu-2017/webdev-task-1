<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="{{meta.charset}}">
    <title>{{title}}</title>
    <meta name="description" content="{{meta.description}}">
    <link rel="stylesheet" href="/index.css">
</head>
<body>
{{> header }}
<main>
    <h2 class="categories__heading">Категории новостей</h2>
    <ul class="categories">
        {{#each categories}}
        <li class="categories__item">
            <a href="{{url}}" class="categories__link">{{name}}</a>
        </li>
        {{/each}}
    </ul>
</main>
{{> footer }}
</body>
</html>
