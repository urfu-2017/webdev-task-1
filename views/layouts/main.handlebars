<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>Желтая пресса</title>
    <link rel="stylesheet" href="/styles.css">
    <script defer src="https://use.fontawesome.com/releases/v5.0.8/js/all.js"></script>
</head>
<body>
<div class="content">
    <header class="header">
        <h1 class="title"><a href="/">Желтая пресса</a></h1>
        <div class="weather">
            <div class="weather__description">
                <div class="weather__city">{{weather.cityName}}</div>
                <img class="weather__image" src="https://www.metaweather.com/static/img/weather/{{weather.current.stateAbbr}}.svg">
            </div>
            <div class="weather__info">
                <div><span class="temp-letter">t </span>{{weather.current.temp}}&deg;С</div>
                <div><img class="weather__wind-image" src="/images/wind.png">{{weather.current.wind}}<sup>м</sup>/<sub>с</sub></div>
                <div class="weather-small-container">
                    {{#each weather.week}}
                    <article class="weather-small">
                        <div class="weather-small__day">{{date}}</div>
                        <div class="weather-small__info">
                            <div>{{temp}}&deg;C</div>
                            <div>{{wind}}<sup>м</sup>/<sub>с</sub></div>
                        </div>
                    </article>
                    {{/each}}
                </div>
            </div>
        </div>
    </header>
    {{{body}}}
    <footer class="footer">
        <div class="footer__author">@mokosha</div>
        <div class="footer__year">2018</div>
    </footer>
</div>
</body>
</html>