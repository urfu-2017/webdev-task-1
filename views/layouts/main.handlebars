<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>Желтая пресса</title>
    <link rel="stylesheet" href="/styles.css">
    <script defer src="https://use.fontawesome.com/releases/v5.0.8/js/all.js"></script>
</head>
<body>
<div class="content">
    <div class="header">
        <h1 class="title"><a href="/">Желтая пресса</a></h1>
        <div class="weather">
            <div class="">
                <div class="weather-description">
                    <div class="weather-city">{{weather.cityName}}</div>
                    <img class="weather-image" src="https://www.metaweather.com/static/img/weather/{{weather.current.stateAbbr}}.svg">
                </div>
                <div class="weather-info">
                    <div><span class="temp-letter">t </span>{{weather.current.temp}}&deg;С</div>
                    <div><img class="weather-wind-image" src="/images/wind.png">{{weather.current.wind}}<sup>м</sup>/<sub>с</sub></div>
                    <div class="weather-small-container">
                        {{#each weather.week}}
                        <div class="weather-small">
                            <div class="weather-small-day">{{date}}</div>
                            <div class="weather-small-info">
                                <div>{{temp}}&deg;C</div>
                                <div>{{wind}}<sup>м</sup>/<sub>с</sub></div>
                            </div>
                        </div>
                        {{/each}}
                    </div>
                </div>
            </div>
        </div>
    </div>
    {{{body}}}
    <div class="footer">
        <div class="footer-author">@mokosha</div>
        <div class="footer-year">2018</div>
    </div>
</div>
</body>
</html>